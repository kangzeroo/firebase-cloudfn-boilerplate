/**
 *
 * @param text
 */
export const lowerCaseMe = (text: string): string => {
    return text.toLowerCase()
}

/**
 *
 * @param text
 */
export const upperCaseMe = (text: string): string => {
    return text.toUpperCase()
}
